services:
  - type: web
    name: nexus-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:10000 --timeout 300 --workers 1
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: PORT
        value: "10000"